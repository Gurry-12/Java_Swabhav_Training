package com.swabhav.evaluation.advancedoop;

/**
 * Question Description:
 * The `finalize()` method is called before an object is garbage-collected. If, inside the `finalize()` block, you deliberately assign the current object's reference back to a static, living variable, does the object resurrect and escape destruction?
 */
public class ResurrectingDeadTest {

    public static void main(String[] args) {
        // Write your test scenarios or proofs here
        
    }
}
