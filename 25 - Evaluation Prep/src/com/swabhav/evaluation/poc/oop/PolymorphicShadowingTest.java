package com.swabhav.evaluation.poc.oop;

/**
 * POC: Advanced: Create a Parent class with `int speed = 50;` and a Child class with `int speed = 100;`. Use a Parent reference to hold the Child object (`Parent p = new Child()`). Print `p.speed`. Prove that methods are polymorphic, but instance variables are completely shadowed!
 */
public class PolymorphicShadowingTest {

    public static void main(String[] args) {
        // Implement the Proof of Concept here
        
    }
}
