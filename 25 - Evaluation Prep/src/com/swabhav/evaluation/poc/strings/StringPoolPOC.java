package com.swabhav.evaluation.poc.strings;

/**
 * POC: Basic: Create `String s1 = "hello"` and `s2 = "hello"`. Prove using `==` that they share the same memory reference. Then create `String s3 = new String("hello")` and prove using `==` that it does not.
 */
public class StringPoolPOC {

    public static void main(String[] args) {
        // Implement the Proof of Concept here
        
    }
}
