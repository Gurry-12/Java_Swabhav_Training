package com.swabhav.evaluation.poc.strings;

/**
 * POC: Intermediate: Prove that `Integer.valueOf(100) == Integer.valueOf(100)` is true, but `Integer.valueOf(200) == Integer.valueOf(200)` is false. Explain Java's -128 to 127 Integer caching mechanism.
 */
public class WrapperCachingPOC {

    public static void main(String[] args) {
        // Implement the Proof of Concept here
        
    }
}
