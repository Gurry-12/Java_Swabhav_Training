package com.swabhav.evaluation.poc.java8;

/**
 * POC: Advanced: Create an Interface with a `default` method `log()`. Create a
 * parent class that has an identically signed `log()` method. Create a Child
 * class that extends the parent AND implements the interface. Prove which
 * `log()` implicitly wins when you call it.
 */
public class InterfaceDefaultStaticTest {

	public static void main(String[] args) {
		// Implement the Proof of Concept here

	}
}
